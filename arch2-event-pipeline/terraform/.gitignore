# terraform 상태/캐시 - 절대 git에 커밋하지 않는다
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.backup
*.tfvars
crash.log

# 실제 CI/CD가 빌드하는 배포용 zip (placeholder zip인 lambda-src/*.zip과는 다름)
# CI/CD 파이프라인이 로컬에서 빌드할 때 생기는 산출물이라 git에는 필요 없다.

